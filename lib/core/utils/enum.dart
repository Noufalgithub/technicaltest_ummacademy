//enum loading
enum LoadingState { initialize, loading, loaded, lazyLoading, error }

//enum snackbar
enum SnackBarType { success, error, warning, info }

// connectivity status
enum ConnectivityStatus { wifi, mobile, offline }
